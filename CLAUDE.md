@AGENTS.md

# HookViral — Vision produit & roadmap

## Positionnement
HookViral n'est PAS un simple générateur de hooks. C'est une **suite d'optimisation de la rétention pour créateurs de short-form** (TikTok / Reels / Shorts).
On ne vend pas une feature ("génère des hooks"), on vend un résultat : **"répare les 3 premières secondes de tes vidéos"**.
Le générateur gratuit est la **porte d'entrée d'acquisition**, pas le produit final.

## Principes directeurs
- **Pas d'auth prématurée.** localStorage couvre historique/favoris. Comptes + DB seulement quand il y a un tier payant à protéger. Le "no login" est un avantage compétitif — le garder le plus longtemps possible.
- **Rate-limiting serveur AVANT tout lancement public payant.** Les crédits localStorage ne protègent pas la clé API.
- **Le moat = SEO programmatique + corpus de patterns possédé**, pas une base de hooks curée à la main.
- **Niche modes = presets de prompt** qui alimentent aussi les pages SEO. Une construction, deux bénéfices.
- **Honnêteté** : ne jamais sous-entendre qu'on mesure la rétention vidéo réelle. Aucune fausse stat, aucun faux témoignage.
- **Soustraction > addition** : chaque ajout doit justifier sa charge cognitive. Defaults > knobs.
- Modèle Anthropic Claude (pas OpenAI/Gemini). Modèle le plus récent disponible (`claude-sonnet-4-6` actuellement).

## Roadmap

| Phase | Focus | Statut |
|---|---|---|
| **0** | Générateur qui marche, rate-limit IP, fuite crédits corrigée | ✅ done |
| **1** | Repositionnement rétention, Hook Analyzer autonome | ✅ done |
| **2** | Niche modes, pages SEO programmatiques, Trends (Google + YouTube) | ✅ done |
| **3** | Rewrite engine, monétisation Stripe | ⏸️ Rewrite ✅ / Stripe **reporté** (décision user 2026-05-18) |
| **4** | Comptes utilisateurs, workspace persistant, analytics | Non démarré |

**Stripe / monétisation : REPORTÉ jusqu'à Phase 4.** Pré-requis : comptes users → rate-limiting durable (Upstash en place) → entitlement serveur → Stripe (compte + clés + priceId fournis par user). Tant que Stripe n'existe pas, `isPro()` retourne `true` pour tout le monde (cf. État actuel).

## Beachhead & moat
- **Marché-beachhead = chaînes faceless / automation** (gros volume, mindset outil/ROI, rétention = revenu direct, peu servi par vidIQ).
- **Anti-clone** : pas les features (Analyzer copiable) mais (1) appropriation niche faceless, (2) corpus de patterns curé qui se compose avec l'usage, (3) SEO programmatique niche.
- **Contrainte honnête** : pas d'accès gratuit aux vraies données de rétention/performance par vidéo. Le score reflète la *vraisemblance structurelle* d'arrêter le scroll, pas une mesure de tes vidéos passées.

---

## État actuel (2026-05-21)

### La boucle visible
Le produit est **un système, pas un tas**. Quatre surfaces dans l'ordre : **Trends (Discover) → Generator (Create) → Analyzer (Diagnose & fix) → Patterns (Learn)** → retour Generator. Le composant `components/NextStep.tsx` rend cette boucle visible en bas de chaque page de la boucle + en mode "outside-loop" sur les pages d'entrée (SEO niches, History, why-it-works) pour éviter les dead-ends.

### Vocabulaire commun : 9 attention patterns
`lib/patterns.ts` — taxonomie possédée. Chaque hook (generated, researched, analyzed) est tagué avec ses `patternsUsed` (whitelist serveur contre `HOOK_PATTERNS`). Les chips de patterns sont visibles sur les cartes du Generator et les angles du Trends research, et linkent `/patterns#<id>`. **Même langue end-to-end.**

### Platform psychology par plateforme
`lib/platforms.ts` — blocks de psychologie par plateforme short-form (TikTok / Reels / YouTube Shorts), structure miroir de `lib/niches.ts` : `slug + label + emoji + tagline + guidance`. Le `guidance` est injecté dans les system prompts de `/api/generate`, `/api/analyze`, `/api/rewrite`, et `decodeTrend` via `platformGuidance(slug)` — Claude juge/génère/réécrit contre les bons critères par plateforme (TikTok = 0-3s pattern interrupt, Reels = aspirational/save-this, Shorts = curiosity loop + CTR). **Scope court terme = short-form uniquement** ; Reddit/X/LinkedIn/Meta-static reportés post-validation user (cf. `[[project_strategic_state]]` en mémoire — décision 2026-05-27).

### Surfaces
- **Home** (`app/page.tsx`) — 6 sections : Hero ("Score your hook in 5 seconds. Fix it in one click." + **3 PersonaCards bordées en grid auto-fit** sous le hero left, mappées directement sur l'état mental du visiteur de "j'ai quelque chose de précis" → "j'ai rien" : `✦ I have a hook` (accent hot, CTA primary gradient "Score it — Free →" → `/analyzer`) + `💡 I have an idea, no hook` (accent gold, CTA outline "Generate 8 hooks →" → `/generator`) + `🔬 I have a niche, no idea` (accent electric, CTA outline "Discover trends →" → `/trends`). Grid `minmax(175px, 1fr)` pour que 3 cards fit en une rangée dans la colonne hero-left ~520px ; stack en mobile via auto-fit. Chaque card = emoji + title + description + bouton, border-left coloré 3px, hover translateY + glow, clickable en entier) · **ScoreDemo** ("Watch the fix happen" — before/after animé 32→87, `components/ScoreDemo.tsx`, SVG pur + CSS vars zéro-dep, rings + compteurs rAF + chips stagger `chipPop`, déclenché par IntersectionObserver, `prefers-reduced-motion` respecté, patterns = vraie taxonomie `lib/patterns.ts` ; placé entre Hero et How-it-works) · **PhoneShowcase** ("Step by step" — dans How-it-works, `components/PhoneShowcase.tsx`, 3 phones en éventail — voir `4a11bd3` SVG pur + CSS vars zéro-dep, rings `stroke-dashoffset` + compteurs rAF + chips stagger `chipPop`, déclenché par IntersectionObserver, `prefers-reduced-motion` respecté, patterns = vraie taxonomie `lib/patterns.ts` ; placé entre Hero et How-it-works = preuve visuelle honnête sans photo stock, comble aussi le vide visuel mobile)) · How-it-works (4 cartes YOU DO/YOU GET) · `HomeAnalyzeWidget` (paste→score réel inline via `/api/analyze`) · Email capture · Final CTA. Pas de FAQ, pas de typing demo, pas de Features grid, pas de testimonials.
- **Generator** (`app/generator/page.tsx`) — Topic + Generate visibles. Platform/Tone/Niche/Goal sous `<details>More options ▾</details>`. Crédit bar `hv_credits` (10/j localStorage). 8 hooks scorés avec `patternsUsed` chips + bouton "Deep retention analysis →" → `/analyzer?hook=`. Le bouton Script ▶ génère vraiment (gating Pro inactif).
- **Analyzer** (`app/analyzer/page.tsx`) — Hook + Analyze visibles. Platform/Niche sous `<details>More options ▾</details>`. Test 3s live sous l'input (heuristique 3.3 mots/s). **Dual-mode auto-détecté via `looksLikeTopic(input)`** : (1) **Score mode** (hook fini) → Score 0-100 + sub-scores + why + weak points + patterns used/missing (chips → `/patterns#<id>`) + rewrite engine (5 styles + "Or fix a missing pattern", 12→84 vérifié, Before/After delta par variante) + callout **🔬 Research the topic** (CTA gradient vers `/trends/research?q=<subject>` quand `/api/analyze` extrait un subject). (2) **🎯 Strategic Takes mode** (input ressemble à un sujet) → branche vers `/api/strategic-takes` au lieu du scoring : Claude + `web_search_20250305` (max 3 searches) produit 2-3 **positions opposées** (emoji + name + stance + reasoning + 3-5 arguments concrets + 2-3 hooks scorés avec patterns chips) + `sources[]`. Bordure gauche colorée par take (rouge/électrique/or) pour distinguer les camps visuellement. Override "Score as a hook anyway →" pour les false positives du detector. SessionStorage cache `hv:takes:<subject>::<niche>` + Upstash 6h. Prefill via `?hook=&platform=&niche=` + auto-run.
- **Trends** (`app/trends/page.tsx`) — Liste + keyword filter + refresh visibles. **Multi-source** : 8 toggles indépendants 🔎 Google · 📚 Wikipedia · 🟧 HN · 🦋 Bluesky · ▶ YouTube · 🟠 Reddit · 🎵 TikTok · 📸 Instagram (✓ devant les actifs). **X (Twitter) retiré de l'UI** depuis `9bc306a` (burn Apify $0.04/item + données démo — code dormant, cf. journal). **Défaut = Google + Wikipedia + Hacker News + Bluesky** (4 sources zéro-auth) ; YouTube, Reddit, TikTok et Instagram restent visibles mais OFF par défaut parce qu'ils requièrent respectivement `YOUTUBE_API_KEY`, `REDDIT_CLIENT_ID`/`SECRET`, et `APIFY_TOKEN` (TikTok + Instagram partagent le même APIFY_TOKEN = payant per-run, donc OFF par défaut protège la facture ; tous deux passent par `guardedApifyRun` = cache-first + verrou NX + cache de l'échec → ≤1 run / clé / 6h) — pas de banners pour un visiteur first-time. Le toggle d'une source gated affiche son banner explicatif on-demand. Sélection persistée via `getSourcesPref`/`setSourcesPref`. Geo (🌍 Global / US / FR / UK / CA / ES / DE) + Niche sous `<details>Filters ▾ (état dans summary, ex: "Google + YouTube · US · Sports")</details>`. **Fetch parallèle** côté serveur (`/api/trends?sources=google,youtube,reddit`) : merge round-robin + dedupe par title lowercased → typiquement ~100 trends uniques quand tout est activé. Chaque carte porte son **source badge** (chip coloré par source : 🔎 Google bleu, 📚 Wikipedia slate, 🟧 HN orange-vif, 🦋 Bluesky bleu-brand, ▶ YouTube rouge, 🟠 Reddit orange, 🎵 TikTok rose-brand, 𝕏 X bleu-twitter-legacy, 📸 Instagram rose-IG) pour distinguer la provenance dans le merge. **Caps par source** : Google = ~10 items par geo (RSS, seul endpoint vivant en 2026) + 30 via "🌍 Global" (fan-out 6 geos // dedupe) · **Wikipedia = 50** (top viewed via Wikimedia REST API officielle, no auth ; le endpoint "top" lag ~24-48h donc on walk-back J-1→J-2→J-3 et on sert le jour le plus récent agrégé — hardcoder J-1 retournait 404/vide chaque jour jusqu'à ~midi UTC ; geo → langue wiki en/fr/es/de, English fallback pour les autres ; filtre namespaces internes Main_Page/Special:/Wikipedia:/etc.) · **Hacker News = 50** (front page via Algolia HN search, no auth, signal tech/biz/startup, géoless) · **Bluesky = 50** (atproto `app.bsky.feed.getFeed` sur le feed curé **"whats-hot"** (était "whats-hot-classic" — feed supprimé par son curateur, détecté via 400 en prod, corrigé `a7f1a30`), `public.api.bsky.app` sans auth, posts tronqués à 140 chars + sub `@handle · ↑likes · 💬replies`, géoless ; signal "what's the discourse" à compléter de Wikipedia/Google) · YouTube = 50 (API officielle, `YOUTUBE_API_KEY` requis) · Reddit /r/popular = 50 (OAuth `client_credentials` requis depuis 2023 sur IPs cloud — `REDDIT_CLIENT_ID` + `REDDIT_CLIENT_SECRET`, token Bearer 1h caché in-memory, NSFW/stickied filtrés) · **TikTok = 50** (two-step géo-aware via `lib/trends.ts:tiktokTrends` : 1️⃣ endpoint gratuit `ads.tiktok.com/creative_radar_api/v1/popular_trend/hashtag/list` avec UA browser + Referer Creative Center, parse `{code:0, data:{list:[…]}}` → `#hashtag` + `formatViews(video_views)` ; 2️⃣ si direct échoue / vide / TikTok rotate ses signed headers msToken/X-Bogus, fallback Apify actor `doliz~tiktok-creative-center-scraper` via `lib/apify.ts:runApifyActor` qui appelle `api.apify.com/v2/acts/.../run-sync-get-dataset-items` — lecture défensive des champs `hashtagName ?? name ?? title` + `video_views ?? views ?? playCount` car le schéma de l'actor drift entre versions. ⚠️ **fallback Apify de fait inerte** : l'actor `doliz~tiktok-creative-center-scraper` a changé de schéma et requiert maintenant des **cookies TikTok Creative Center** qu'on ne peut pas fournir → il échoue/renvoie vide. TikTok dépend donc en pratique du seul endpoint direct. Tout passe désormais par `guardedApifyRun` (clé `tiktok:<country>`) : cache-first + verrou NX + **cache de l'échec** 6h → si direct ET Apify échouent, `[]` est caché 6h (plus de re-run à chaque rendu — c'était la fuite TikTok corrigée `a4b44f5`). `missing-key` (pas de token) reste non caché → `configured: false`) · **X (Twitter) — ⛔ DÉSACTIVÉ `9bc306a`** (hard-gaté hors `parseSources` + retiré de l'UI ; `twitterTrends` dormant en code. Raison : actor `apidojo~twitter-scraper-lite` facture $0.04/item ET renvoie du démo sur plan FREE → $3.20 brûlés sur $5. Réactiver seulement sur plan payant. Specs d'origine ci-dessous pour la réactivation : niche-aware géoless via `lib/trends.ts:twitterTrends(nicheSlug)` — l'API X officielle est payante depuis 2023 donc passage par scraper tiers : appel isolé derrière `fetchTwitterViaProvider()` pour pouvoir swapper Apify → reseller REST (GetXAPI, TwitterAPI.io) sans toucher le reste de la source ; actor `apidojo~twitter-scraper-lite` avec `{searchTerms, sort:"Top", maxItems:50, tweetLanguage:"en"}` ; `TWITTER_NICHE_TERMS` = map niche→3 hashtags (#fitness/#workout, #finance/#investing, etc.) avec fallback `["#trending"]` sans niche ; lecture défensive `text/likeCount/retweet/viewCount/author.userName/createdAt` car le schéma actor drift ; mapping `title` = tweet tronqué word-boundary à 100 char + "…" via `truncateAtWord()`, `sub` = `${formatCount(likes)} likes · X` ; cache 6h via `getApifyCache`/`setApifyCache` (Upstash partagé cross-instance + `apifyMemCache` per-instance en hot-path), clé `tr:apify:twitter:<niche>` — 1 run max / niche / 6h toutes instances confondues ; APIFY_TOKEN requis, payant per-run, OFF par défaut comme TikTok) · **Instagram = 30** (niche-aware géoless via `lib/trends.ts:instagramTrends(nicheSlug)`, posts PUBLICS uniquement — pas de profils privés/stories/DM, données accessibles à n'importe quel visiteur sans login ; appel isolé derrière `fetchInstagramViaProvider()` pour swap futur vers Graph API ou autre scraper ; actor officiel `apify~instagram-scraper` avec `{search: string, searchType:"hashtag", resultsType:"posts", resultsLimit:30, addParentData:false}` — **`search` doit être un string** (pas un array — validé en prod : 400 "Field input.search must be string", corrigé `a7f1a30`) ; `INSTAGRAM_NICHE_HASHTAG` = map niche→1 hashtag sans `#` (un seul par niche, 3× moins cher que les 3 prévus initialement), fallback `"reels"` sans niche ; lecture défensive `caption/url/ownerUsername/likesCount/commentsCount/videoViewCount/timestamp/hashtags` ; mapping `title` = caption tronquée word-boundary 100 char OU `#{firstHashtag}` si pas de légende (posts image-only) — drop si ni l'un ni l'autre ; `sub` = `${formatCount(likes)} likes · Instagram` SAUF si `likesCount === -1` (poster a caché ses likes via setting privacy IG 2019) → fallback `"Instagram"` seul ; cache 6h via `getApifyCache`/`setApifyCache` (Upstash partagé cross-instance + `apifyMemCache` per-instance en hot-path), clé `tr:apify:instagram:<niche>` — 1 run max / niche / 6h toutes instances confondues ; APIFY_TOKEN requis, payant per-run, OFF par défaut comme TikTok/X). **Geo row désactivé seulement quand TOUTES les sources sélectionnées sont géoless** (Reddit + HN + Bluesky + Instagram) ; mixé avec Google/YouTube/Wikipedia → geo s'applique aux géo-aware. Velocity badges (🔥 Rising / ⚡ Steady / 📉 Cooling / 🆕 New) + sparkline d'historique de rang calculés par source quand Upstash configuré et ≥3 snapshots. Compteur "N trends" toujours visible. Banner doré "YOUTUBE_API_KEY manquante" affiché seulement si YouTube est dans la sélection et `unconfigured`. Carte trend simplifiée : CTA gradient **🔬 Research & angles** (Pro visuel) en premier → navigue vers `/trends/research?q=&niche=` ; en dessous, deux raccourcis discrets ⚡ Quick hooks (Generator) + ✦ Score it (Analyzer).
- **Trends Research** (`app/trends/research/page.tsx`) — Page dédiée, layout 2 colonnes desktop (Context sticky 42% / Angles 58%) + stack mobile. Claude + `web_search_20250305` tool (max 3 searches/call) → `context {who/what/whyTrending/stakes/timeline}` à gauche + 3-5 angles scorés à droite (chacun avec `reasoning` + chips patterns + 4 boutons : Copy hook / ⚡ More hooks / ✦ Analyze / 📋 Faceless brief) + bandeau `sources[]` URLs cliquables. **3 niveaux de cache** : Upstash 6h (`tr:research:<niche>:<trend>` partagé serveur) + mémoire process + **sessionStorage tab-scoped** (`hv:research:<q>::<niche>`) pour restauration instantanée au retour en navigation (zéro loading flash quand tu reviens depuis Analyzer). Bouton "← Back to trends" = `<Link href="/trends">` fixe, prévisible. Loading state explicite ("Searching the web for X… ~10s") + retry sur erreur. Accédée depuis Trends *et* depuis le résultat Analyzer (via subject extrait).
- **Patterns** (`app/patterns/page.tsx`) — Hub : chaque pattern a "See N examples →" (vers la page dédiée) + "✦ Analyze a hook for this" + "⚡ Generate hooks like this". Anchors `#<id>` ciblées par les chips Analyzer. **Pages dédiées `/patterns/[id]`** (`app/patterns/[id]/page.tsx`, 9 SSG via `generateStaticParams`) : chaque pattern = page SEO rankable sur sa long-tail ("open loop hook examples", "curiosity gap tiktok hook") — hero why-it-works + 6 exemples scorés (`examples[]` dans `lib/patterns.ts`, chacun linke `/analyzer?hook=` pour un score live) + how-to-apply + cross-links vers les 8 autres patterns.
- **Start here** (`app/why-it-works/page.tsx`) — Guide plain-language : 4 outils × (What does it / When to use / Why it works) en 1 ligne chacun. Remplace l'ancien article méthodologie scientifique. URL conservée pour les liens entrants existants.
- **Pages SEO** : `/hooks-for/[niche]` (exemples evergreen) + `/trends/[niche]` (live + JSON-LD FAQPage/ItemList + contenu evergreen). Cross-linkées. Toutes deux ont NextStep en outside-loop. **Pages plateforme** (statiques manuelles, structure parallèle) : `/tiktok-hook-generator` + `/youtube-hook-generator` + `/instagram-hook-generator` — hero + exemples par niche + 6 formulas plateforme (sourcées `lib/platforms.ts`) + how-to + FAQ + maillage croisé "More Hook Generators" ; CTAs → `/generator?platform=tiktok|reels|shorts`.
- **Couche SEO technique** (2026-05-29) : `app/layout.tsx` porte la metadata racine complète — `metadataBase: https://hookviral.ai`, title `template "%s | HookViral AI"`, description riche, **keywords array exhaustive** (hook generator / analyzer / tiktok-reels-shorts / retention / faceless …), `openGraph` + `twitter summary_large_image` + `robots` (index/follow, googleBot max-image-preview large) + JSON-LD `@graph` Organization + WebSite + SoftwareApplication (offer price 0). `app/sitemap.ts` (toutes surfaces + fan-out `NICHE_MODES` → 2 URLs/niche) et `app/robots.ts` (allow /, disallow /api, sitemap + host) générés par Next (`/sitemap.xml` + `/robots.txt` au build). Les pages **client** (analyzer, generator, trends, trends/research, pricing) ne peuvent pas exporter `metadata` → chacune a un `layout.tsx` serveur qui porte title/description/keywords/canonical/OG propres. `hooks-for/[niche]` a maintenant un `keywords` array par niche. Domaine canonique = `hookviral.ai` (à connecter si encore sur le preview vercel).
- **History** (`app/history/page.tsx`) — Sessions Generator sauvegardées localStorage. Chaque hook a "✦ Improve →" vers Analyzer prefill. Empty state → Trends.
- **Saved** (`app/saved/page.tsx`) — Watch-list de trends (⭐), **distincte de History** (History = créé ; Saved = surveillé). localStorage `hv_saved_trends` (array `{id, title, source, url, savedAt}`, `id = title.toLowerCase()`) + helpers `lib/prefs.ts`. Le toggle ⭐ vit sur chaque carte Trends (absolute top-right, gold si sauvé). La page liste les sauvés en même format de carte que Trends (3 CTAs Research/Hooks/Score, niche-less) + ✕ remove + empty state → /trends. Layout SEO `noindex` (page perso localStorage). Badges de source partagés via `lib/sourceBadges.ts` (data pure, importée par Trends + Saved).
- **Pricing** — 5 puces Free / 5 puces Pro. Trust note : "Same AI on both plans. Pro = unlimited."

### Nav
`Analyze · Generate · Trends · Patterns · Pricing` + dropdown **Library** (Saved trends / History / Start here) + CTA "Score my hook" — depuis le redesign 2026-07-03. Les pages perso localStorage (Saved/History) ne pèsent plus autant que les outils dans la rangée principale ; `/why-it-works` (Start here) est enfin accessible depuis la nav. Analyze reste en tête (flagship + demo qui convertit), Generate suit ("j'ai un hook → score-le" avant "page blanche → génère"). Drawer mobile = liste flat complète (Home + 5 + Library). Le curseur custom (ring + MutationObserver) a été **supprimé** au redesign — gadget de template, invisible en light, coût perf réel. Note : `NextStep` garde son propre ordre pédagogique de boucle (`Trends → Generate → Analyze → Patterns`) — intentionnellement différent de la nav.

### État Pro (pré-monétisation)
`lib/plan.ts` : **`isPro()` retourne actuellement `true`** (commentaire explicatif dans le fichier). Conséquence : Script generator, rewrite 5 styles × 3 variantes, decode illimité, faceless brief — tout débloqué. Tous les locks/badges/teasers Pro ne rendent jamais via les conditionnels `!isPro()`. Le code de gating est intact ; ungate-by-default = une seule ligne à reflipper quand Stripe arrive (`localStorage.getItem("hv_plan") === "pro"`).

### Préférences cross-page
`lib/prefs.ts` (localStorage `hv_niche` / `hv_geo`) — Trends + Analyzer hydratent et persistent. Generator a sa propre liste niche legacy non-NICHE_MODES, non refactorisée (deep-links `?niche=` continuent de fonctionner).

### Infra durable
`lib/upstash.ts` (REST fetch, zéro dépendance npm) + `lib/rateLimit.ts` async sliding-window. **Inactif tant que `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` absents de `.env.local`** → fallback in-memory. Active automatiquement quand les clés sont présentes. Mêmes clés alimentent `/api/subscribe` (capture email → `SADD hv:subscribers` + `HSET hv:subscriber_meta`, 503 honnête si non configuré), `computeVelocity` (snapshots de rang trends `tv:<source>:<geo>:<bucket>` + `tvh:<source>:<geo>`), `getCachedResearch` (`tr:research:<niche>:<platform>:<trend>`), et **`getApifyCache`/`setApifyCache`** (`tr:apify:<source>:<key>` pour TikTok/Twitter/Instagram — protège le budget Apify : 1 run max / (source, key) / 6h cross-instance ; sans Upstash configuré → fallback `Map` per-instance qui re-hit Apify à chaque cold start).

### Branding & design system (depuis 2026-07-03)
**`DESIGN-SYSTEM.md` est la référence obligatoire pour toute modification UI** (issu de l'audit `AUDIT.md` + plan `FIXES.md`). Essentiel : **une seule couleur signature** `--accent` (framboise #C9134B light / #FF3D71 dark) ; vert/or/rouge = *sémantiques* (tiers de score via `lib/score.ts:scoreColor` — ≥85 success / 60-84 warning / <60 danger — définition unique, les 4 copies locales divergentes ont été supprimées) ; le dégradé `--brand-grad` (accent→violet) ne vit que sur le wordmark ; les anciennes vars (`--hot`, `--electric`, `--neon`, `--gold`, `--s1`…) sont des **alias legacy** dans globals.css — ne plus les utiliser en code neuf. **Icônes = Lucide uniquement** via `lib/icons.tsx` (`<Icon name="…"/>`, 16/20px, stroke 1.75) — zéro emoji dans le chrome UI. Composants partagés `components/ui.tsx` (Button/Spinner) + classes CSS `.btn/.chip/.card/.kicker` (hover/focus en CSS, plus de `useState(hov)` pour les nouveaux composants). Fonts **Syne + DM Sans via `next/font`** (plus d'@import Google). **Wordmark** "HookViral" — "Hook" en `--brand-grad`, "Viral" en `--text` (`components/Logo.tsx`). **Favicon** `app/icon.svg` inchangé. Nav header : Logo + suffixe `.ai` discret.

### Thème (light par défaut depuis `5f4d3d6`)
**Défaut = light**, dark en opt-in (`ThemeProvider` retourne `"light"` sans préférence sauvée ; bascule manuelle ☀/🌙 dans la Nav). `globals.css` : `:root` nu (= SSR sans attribut) porte la palette light → zéro flash pour le visiteur SEO par défaut ; `[data-theme="dark"]` placé en dernier gagne à spécificité égale ; tokens structurels (fonts/sizes/radii) dans un `:root` indépendant du thème. **Les deux palettes passent WCAG AA 4.5:1** sur le texte principal (light worst-case `--hot` 4.67 ; dark déjà fixé via les commits Lighthouse). Note : les anciennes entrées journal qui disent "Lighthouse audite en thème par défaut [dark]" sont historiques — le défaut est maintenant light.

### Lighthouse prod
Mesures sur `hookviral-ai.vercel.app` : **Performance 100 · Best Practices 100 · SEO 100 · Accessibility 100** (cible atteinte post dark-mode contrast fix). Core Web Vitals : FCP 317ms, LCP 358ms, Speed Index 430ms, CLS 0. Note durable : **toujours run Lighthouse sur prod, pas localhost** — le dev mode gonfle artificiellement par +218KB de devtools + JS non-minifié + HMR runtime. **Browserslist ciblé** (chrome ≥93 / firefox ≥92 / safari ≥15.4 / edge ≥93) pour éviter les polyfills inutiles (Array.at, Array.flat/flatMap, Object.fromEntries/hasOwn, String.trimEnd/trimStart) — économise ~14 KB sur le bundle principal `chunks/16g…js`.

---

## Trous restants honnêtes (forward-looking)
- **Generator niche-aware** : liste niche legacy à migrer vers `NICHE_MODES` pour partager les prefs avec Trends/Analyzer. Non urgent.
- **History incomplète** : capture seulement les sessions Generator, pas les Analyzer + Rewrite (résultats 84/+72 perdus). Nécessite store persistant (Phase 4).
- **Enforcement Pro serveur** : aujourd'hui c'est soft-gating client uniquement. Quand Stripe arrive (Phase 4) → entitlement check sur chaque route Pro avant Claude call.
- **Témoignages** : aucun (les faux ont été retirés). À remplacer par de vrais quand ils arrivent.
- **TikTok trending** : aucune source officielle gratuite fiable. Approche honnête retenue : Google/YouTube comme proxys + l'utilisateur peut coller un trend TikTok repéré dans Decode.

## Dette technique
- `hv_credits` (Generator) pas migré vers `lib/plan.ts` — deux compteurs quotidiens coexistent (`hv_credits` + `hv_free_decode`). Pas bloquant.
- Logique de crédits dupliquée entre `generator/page.tsx` et `lib/credits.ts` (la lib n'est pas utilisée).
- `lib/prompt.ts` non importé (les routes API ont leurs propres prompts inline, plus complets).
- Parsing JSON fragile dans les routes API restantes (script utilise encore `JSON.parse(raw.replace(/```json/, ""))`). `lib/parseJson.ts` `extractJson` existe et est utilisé dans `generate`, `analyze`, `rewrite`, `rerankForNiche`, `decodeTrend`, `strategic-takes`. À propager dans `script` quand pertinent.
- `ANTHROPIC_API_KEY` requis en `.env.local` pour tester end-to-end localement.

## Action user en attente
- Ajouter `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` dans `.env.local` (Redis gratuit sur upstash.com) pour activer le rate-limiting durable, la vélocité des trends et la capture email. Sans ça : fallback in-memory + form email = 503.
- `YOUTUBE_API_KEY` (optionnel, Google Cloud gratuit) pour activer la source YouTube filtrable par niche dans Trends. Google fonctionne sans rien.
- `REDDIT_CLIENT_ID` + `REDDIT_CLIENT_SECRET` (optionnel, gratuit) pour activer Reddit. Depuis le tightening API de mi-2023, Reddit 403/429 les IPs cloud (Vercel/AWS) sans OAuth — le JSON public ne marche plus qu'en navigateur. Register : reddit.com/prefs/apps → "script" app → CLIENT_ID = sous le nom de l'app, SECRET = bouton "secret". Banner explicatif affiché dans `/trends` quand Reddit est sélectionné sans creds.
- `APIFY_TOKEN` (optionnel, **payant per-run**) pour **Instagram** (source unique via `apify~instagram-scraper`, hashtag search posts publics ~$0.0027/result). **TikTok** consomme aussi le token *en théorie* (fallback `doliz~...`) mais cet actor exige désormais des cookies CC → de fait inerte, TikTok = direct endpoint seul. **X (Twitter) est DÉSACTIVÉ** (`9bc306a`) : l'actor `apidojo~twitter-scraper-lite` facture **$0.04/item** et renvoie du **démo** sur plan FREE → a brûlé $3.20/$5 en un test. ⚠️ Leçon : les actors Apify « pay-per-result » facturent **même les items démo** ; ne réactiver X que sur un plan Apify payant. Toutes les sources Apify passent par `guardedApifyRun` (cache-first + verrou NX + cache de l'échec) → **≤1 run / clé / 6h** garanti, mais ça plafonne la fréquence, PAS le coût par run (d'où le hard-gate de X). Protection conseillée : mettre une **limite de dépense à $0** dans apify.com → Billing → Limits tant qu'on n'a pas de plan payant. **Mode dev** : poser `APIFY_DISABLE=1` dans `.env.local` (+ redémarrer `npm run dev`) → aucun run Apify lancé, on sert le dernier cache ou `configured=false` ; permet de développer/recharger la page sans jamais consommer de crédit (`7b0a372`). Get token : apify.com → Settings → Integrations.
- *(rien en attente sur Lighthouse — Accessibility 100 atteint via dark-mode contrast fix.)*

---

## Journal des changements
Tous les changements et améliorations significatifs, du plus récent au plus ancien. Une ligne par commit. Le détail vit dans `git show <sha>`.

**2026-07-03**
- **Passe de finition visuelle** (feedback user "remplace les emojis par des icônes, il faut un Home, apporte un plus") : (1) **Image OG/Twitter générée au build** (`app/opengraph-image.tsx` + `twitter-image.tsx` via `next/og`/satori, fonts statiques dans `assets/fonts/` — satori ne gère ni les variable fonts ni `<text>` SVG ni `inset`, chiffre overlayé en HTML) — chaque partage de lien affiche maintenant wordmark + headline + carte score ring 87 au lieu d'une carte vide. (2) **Favicon** `app/icon.svg` réaligné sur la nouvelle DA (framboise→violet). (3) **Home explicite dans la nav desktop** (icône maison avant les 5 entrées). (4) **Icônes là où les emojis avaient laissé du texte nu** : `NICHE_ICONS` map dans `lib/icons.tsx` (fitness→dumbbell, finance→wallet…) → chips niche Analyzer/Trends + heros et chips "More niches" de `/hooks-for` et `/trends/[niche]` (tuile 56px accent-soft) ; toggles sources Trends = point de couleur brand + check ; badges source des cartes = dot + label ; Global = icône globe ; titre Saved = tuile étoile. (5) **`components/ScoreRing.tsx` extrait** et utilisé dans le résultat Analyzer (le moment flagship a l'anneau animé, plus un chiffre nu) + widget home + ScoreDemo — une seule implémentation du visuel signature. (6) Tuiles d'icônes accent-soft sur PersonaCards et cartes How-it-works, voile radial accent statique unique derrière le hero (remplace les ex-orbes), **vrai footer** (wordmark + tagline + colonnes Product/Learn/Library + légal — maillage interne vers les pages SEO depuis chaque page).
- `f74abf4` — **Refonte design system complète** (audit DA "site généré par IA" → exécution items 2-15 de `FIXES.md` ; item 1/Stripe explicitement différé par le user — l'`alert()` du bouton Pro est intact). Nouveaux docs de référence : `AUDIT.md` (audit impitoyable), `FIXES.md` (plan priorisé), **`DESIGN-SYSTEM.md` (référence permanente pour toute modif UI)**. Concrètement : (1) **palette réduite à un accent** framboise + sémantiques score via `lib/score.ts` (scoreColor unifié — 4 barèmes divergents supprimés, le même score a la même couleur partout) ; anciennes vars aliasées dans `globals.css` (tous les `gradient(hot,electric)` inline deviennent un aplat accent sans toucher chaque fichier) ; échelles typo/spacing/radius en tokens. (2) **Zéro emoji UI** : ~150 emojis-icônes remplacés par Lucide via `lib/icons.tsx` ; emojis retirés des exemples de hooks (`lib/niches.ts`, `lib/patterns.ts`, pages SEO) ; drapeaux/toggles/velocity/étoiles → texte + icônes. (3) **Copy** : comparatif "✕ ChatGPT / ✓ HookViral" supprimé (était invisible en light de toute façon), titres staccato réécrits ("One plan that unlocks everything", "Stop losing viewers in the first 3 seconds", modal "Go Pro. Go Viral. 🚀" → copy honnête sur les crédits), gradient-text limité au H1 home et statique, "in 2025" → 2026. (4) **Ménage template** : curseur custom supprimé (Nav), orbes floutés supprimés (4 pages), hovers calmés, `page.module.css` starter mort supprimé, keyframes inutilisées purgées. (5) **Plateformes = short-form only** : Generator/Analyzer listent TikTok/Reels/Shorts depuis `lib/platforms.ts` (LinkedIn/X retirés — contredisaient le positionnement sans psychology block) ; prefill `?platform=` accepte slug OU label (les deep-links des pages SEO étaient silencieusement ignorés par le Generator). (6) **Nav 5 entrées + Library** (Saved/History/Start here). (7) **A11y/SEO** : FAQ pricing en vrais `<button aria-expanded>`, aria-labels (hamburger, theme, star, refresh), `:focus-visible` global, `outline:none` retirés, fonts via `next/font` (plus de request Google render-blocking), metadata + noindex sur privacy/terms/history (héritaient `canonical: "/"`), badge "MOST POPULAR" retiré (plan unique), badges PRO retirés tant qu'`isPro()=true`, credit bar Generator allégée (1 ligne au lieu de 4 rappels). Build 47 pages clean, tsc 0, eslint 0, vérifié light + dark au navigateur.

**2026-06-05**
- `17c3752` — **Pages SEO dédiées `/patterns/[id]` avec sets d'exemples scorés** (Option B du benchmark concurrent HookUGC — transformer la taxonomie possédée en SEO programmatique, même levier que `/hooks-for/[niche]`). Chaque pattern devient une page rankable sur sa long-tail propre ("open loop hook examples", "curiosity gap tiktok hook", etc.). `lib/patterns.ts` : chaque `HookPattern` gagne un `examples[]` de 5 hooks scorés couvrant différentes niches (`PatternExample = text + score + niche label`) ; le `example` singulier reste le headline (carte hub + deep-link Generator). Nouvelle route `app/patterns/[id]/page.tsx` : `generateStaticParams` sur `HOOK_PATTERNS` (9 pages SSG prérendues), metadata + keywords par pattern, hero (why-it-works) + 6 exemples scorés (chacun linke `/analyzer?hook=` pour un score live → fait entrer le trafic SEO dans le produit) + how-to-apply + cross-links vers les 8 autres patterns. Hub `/patterns` : chaque carte gagne un lien "See N examples →" ; les ancres `#<id>` que ciblent les chips Analyzer sont inchangées. Sitemap fan-out les 9 URLs pattern (priority 0.6). Build clean (tsc 0, eslint 0, 9 pages prérendues).
- `34dfa4e` — **Pages SEO plateforme : `/youtube-hook-generator` + `/instagram-hook-generator`** (Option A du benchmark HookUGC — compléter la trilogie plateforme, `/tiktok-hook-generator` existait déjà). Chaque page mirror la structure TikTok : hero + exemples de hooks par niche + 6 formulas spécifiques plateforme + article how-to + FAQ + liens internes croisés. Psychologie plateforme sourcée de `lib/platforms.ts` (curiosity loops / tested authority pour Shorts ; transformation arcs / save-this energy pour Reels). CTAs pointent `/generator?platform=shorts|reels` (prefill plateforme). Fix bonus : lien mort `/viral-hook-examples` dans la page TikTok → `/hooks-for/fitness`. Les 2 pages ajoutées au sitemap (priority 0.8). Maillage interne "More Hook Generators" entre les 3 pages plateforme.
- `4a11bd3` — **PhoneShowcase : cluster en éventail de 3 phones dans How-it-works** (remplace la grille texte 100% de How-it-works par une proof visuelle honnête — vraies surfaces produit, pas de photos stock). Architecture : `components/PhoneFrame.tsx` = bezel réutilisable pur CSS (status bar réaliste 9:41 + barres signal + batterie, notch, home indicator, glow halo, zéro dep) ; `components/PhoneShowcase.tsx` = 3 écrans (Trends feed 4 sources, Generator 4 hooks scorés, Analyzer score + sous-scores + patterns + pill "Rewrite to 91 →") + logique fan (client, IntersectionObserver). Fan desktop : centre Generator plus grand (268px) + élevé, côtés (232px) avec overlap et z-index, 3 transforms empilés sur nœuds séparés (élévation / entrée-scale / float) pour éviter collision. Entrée staggerée au scroll (fade + scale). Float continu déphasé par phone (`phoneFloat` keyframe). Status bar réaliste. `≤820px` : strip swipeable scroll-snap + overlap reset propre via `@media`. `prefers-reduced-motion` : pas de float, entrée instantanée. Glow ambiant derrière le cluster (dégradé radial purple/hot). `PhoneFrame` est prévu pour être droppable sur les pages SEO `/hooks-for/[niche]` (text-only, fort levier) — pas encore fait.

**2026-06-03**
- `f36861f` — **Mobile UX ≤480px** : premier bloc `@media` de `globals.css` (le layout était 100% fluide jusque-là) + hooks `className` (les inline styles gagnent la cascade → `!important` scopé mobile, desktop intact). (1) **Trends** : source row (8 toggles ~720px) + geo row débordaient et étaient clippés → stack + scroll horizontal interne via `min-width:0` + `max-width:calc(100vw - 2.5rem)` au lieu d'élargir la page (vérifié 390px : pill clientW 348, scroll interne, zéro overflow page). (2) **Analyzer** : grid patterns "uses vs missing" passe en 1 colonne (au lieu de 2 colonnes ~135px serrées). (3) **Home** : 3 PersonaCards + ScoreDemo before/after passent en 1 colonne ≤480px (évite rangée de 2 + orphelin sur 414-480px). Vérifié au navigateur via émulation device 390px + injection des classes.
- `7185ad7` — **ScoreDemo before/after animé sur Home** (montrer le produit, pas de photos stock — principe d'honnêteté). Nouvelle section "Watch the fix happen" entre Hero et How-it-works : un vrai hook faible scoré 32 avec les patterns qu'il rate, à côté de sa réécriture scorée 87 avec ces patterns présents. Comble le vide visuel mobile (le phone mockup du hero est masqué ≤700px). `components/ScoreDemo.tsx` = SVG pur + CSS vars, **zéro dépendance** : rings `stroke-dashoffset` animés, compteurs `requestAnimationFrame`, chips stagger via keyframe `chipPop` + `animationDelay`, le tout déclenché une fois par `IntersectionObserver`. `prefers-reduced-motion` saute à l'état final. Noms de patterns = vraie taxonomie `lib/patterns.ts` (Open Loop, Concrete Specificity, Stakes, Knowledge Gap). Placement validé par user (option "après le Hero").
- `c2572d3` — **Saved trends (signet ⭐)** : watch-list distincte de History (History = ce qu'on a créé ; Saved = ce qu'on surveille). localStorage `hv_saved_trends` + helpers dans `lib/prefs.ts` (`getSavedTrends/saveTrend/unsaveTrend/isTrendSaved`, `id = title.toLowerCase()` pour dédup + toggle stables). (1) Carte Trends : toggle ⭐ discret (absolute top-right), gold si sauvé, scale au clic, hydraté par carte depuis localStorage. (2) Nouvelle page `app/saved/page.tsx` : même format de carte que Trends (3 CTAs Research/Hooks/Score), ✕ pour retirer, empty state → /trends, layout SEO `noindex` "Saved Trends". (3) Nav : "Saved" entre Trends et Generate (on sauvegarde depuis Trends). (4) `SOURCE_BADGES` extrait dans `lib/sourceBadges.ts` (data pure, zéro dep) pour partage Trends ↔ Saved sans tirer l'import Anthropic serveur de `lib/trends.ts`. Archi + placement Nav validés par user avant exécution.
- `d1a83c2` — **Sous-titres H1 des 3 pages principales alignés sur why-it-works** : Analyzer/Generator/Trends avaient chacun un sous-titre mais le copy avait dérivé (celui du Generator était salesy "Copy. Post. Win."). Re-sourcés depuis les lignes "what it does" de `app/why-it-works`, structure parallèle input→outputs, une ligne sobre chacun, même `<p>` + style (layout intact). Generator → "Pick a topic and niche. Get 8 scored hooks with the attention patterns that make each one work.", Trends → "See what's moving across platforms right now — then turn any trend into a hook angle in one click.", Analyzer → "Paste any opening line — get the score, the missing patterns, and a stronger rewrite in seconds."
- `5f4d3d6` — **Thème par défaut = light (dark devient opt-in)** : les visiteurs SEO arrivent en plein jour sur mobile, le light est plus approchable au premier contact. Les 4 défauts `"dark"` du `ThemeProvider` (contexte, fallback + catch de `getSnapshot`, `getServerSnapshot`) passent `"light"`. `globals.css` restructuré : le `:root` nu (SSR + pré-hydration, sans attribut `data-theme`) porte la palette light → zéro flash pour le visiteur par défaut ; seuls ceux qui ont sauvé "dark" voient un bref flip. Tokens structurels (fonts/sizes/radii) extraits dans un `:root` indépendant du thème ; couleurs dark déplacées dans `[data-theme="dark"]`, placé en dernier pour gagner à spécificité égale. Bascule manuelle inchangée. **Tous les vars texte light vérifiés WCAG AA 4.5:1** (worst-case `--hot` 4.67 sur `--s3`).
- `7b0a372` — **Mode dev `APIFY_DISABLE=1`** (kill-switch crédits) ajouté à `guardedApifyRun`, en complément de la garde budget. Une ligne après le check cache-first et avant le verrou/run : si `APIFY_DISABLE=1` est dans l'env, AUCUN run actor n'est lancé → on sert le dernier cache Upstash s'il existe, sinon `missing-key` → `configured=false`. Couvre TikTok + Instagram (les deux passent par `guardedApifyRun`). Usage : ajouter `APIFY_DISABLE=1` dans `.env.local` + redémarrer `npm run dev` (Next ne hot-reload pas `.env.local`) → recharger la page autant qu'on veut sans consommer un crédit. Vérifié (flow test) : 10 reloads d'une source = 1 run max ; 0 run avec `APIFY_DISABLE=1` (cache miss → throw, cache hit → sert le cache).
- `9bc306a` — **Désactiver la source X (Twitter)** suite à un emballement de coûts Apify (décision user "désactiver X seulement"). Diag facture : `apidojo~twitter-scraper-lite` facture **$0.04 par dataset item** ET renvoie des items **démo** (`{demo:true}`) sur le plan FREE → un run 50-items = ~$2 pour rien ; **$3.20 brûlés sur $5 en une session de test** (80 items × $0.04 = 99% de toute la conso Apify ; TikTok n'a coûté que $0.01 en "actor start", Instagram $0.02). La garde budget plafonne la fréquence mais PAS le coût par run, donc seule issue : rendre la source injoignable. `"twitter"` hard-gaté hors de `parseSources` dans `app/api/trends/route.ts` (le chokepoint unique vers `twitterTrends` — bloque même une URL `?sources=twitter` bricolée) + retiré du toggle row, `ALL_SOURCES`, `parseSourcesCsv`, `allGeoLess`, et son banner. `twitterTrends()` + sa branche fan-out restent en code, dormants ; réactivation = re-ajouter `"twitter"` aux 2 endroits, mais seulement sur un plan Apify payant. TikTok + Instagram restent actifs derrière la garde budget.
- `a4b44f5` — **Garde anti-budget Apify** (`guardedApifyRun` dans `lib/trends.ts`) pour TikTok + Instagram. Avant : le chemin d'échec de TikTok shuntait l'écriture cache → re-run à chaque rendu ; et les rechargements concurrents rataient tous le cache pas-encore-écrit → runs payés dupliqués en parallèle. 3 gardes : (1) cache-first — un `[]` caché compte comme hit, donc un actor démo/cassé arrête de re-facturer pendant 6h ; (2) verrou NX `SET tr:apifylock:<key> NX EX 120` — une rafale de reloads = 1 seul run, les autres skip et retournent `[]` ; (3) cache du résultat succès OU vide. `missing-key` (pas de token) = seul cas non caché + verrou relâché → activation instantanée dès qu'un token est ajouté. Vérifié contre Upstash live : 10 tentatives concurrentes de verrou → exactement 1 `OK`, 9 `null`. Garantie : **≤1 run actor payé / (source,clé) / 6h**, peu importe le trafic.
- `e6d1f98` — **Fix crash `URIError: URI malformed` sur titres avec emoji** : un titre Bluesky tronqué à 140 chars pouvait couper une paire de surrogates UTF-16 en deux → surrogate orphelin → `encodeURIComponent` crashait dans `TrendCard`, tuant le rendu de toute la grille. Racine : `stripLoneSurrogates()` (Safari-safe, sans lookbehind) nettoie la coupe Bluesky 140-char + `truncateAtWord` (X/Instagram). Filet universel : `safeEncode()` aux 3 liens de la page Trends + le lien Generator de `/trends/[niche]` strippe les surrogates avant encodage. Le titre affiché garde ses emoji ; seul le query param de l'URL les perd.

**2026-05-29**
- `a7f1a30` — **Fix Bluesky 400 + Instagram input schema error** (bugs détectés en prod au premier `npm run dev`). (1) **Bluesky** : le feed `whats-hot-classic` a été supprimé par son curateur (400 `InvalidRequest "could not find feed"` — exactement le risque documenté dans le code). Probe live confirmé : `whats-hot` sur le même DID répond 200 → swap d'URI, une ligne. (2) **Instagram** : l'actor `apify~instagram-scraper` refuse les arrays pour le champ `search` (400 "Field input.search must be string") — la spec que j'avais documentée était incorrecte. Fix : `INSTAGRAM_NICHE_HASHTAGS: Record<string, string[]>` → `INSTAGRAM_NICHE_HASHTAG: Record<string, string>`, un hashtag par niche, `fetchInstagramViaProvider()` mis à jour en `(search: string)`. Bonus : 3× moins cher par run Apify (1 hashtag au lieu de 3).
- `0b32f4d` — **Cache Apify durci via Upstash** (protection facture, demande user après ajout des 3 sources Apify-backed) : le `Map` per-instance que j'avais mis pour TikTok/Twitter/Instagram ne protégeait PAS contre le scénario serverless principal — chaque cold start spawn une nouvelle instance avec Map vide, donc 10 instances chaudes en parallèle = jusqu'à 10 runs Apify pour la MÊME donnée dans la même fenêtre 6h. Avec $5 de crédits Apify et un actor IG à $1.50/1000, c'est le genre de bug qui te ruine en silence. Fix : helpers génériques `getApifyCache(key)` / `setApifyCache(key, data)` calqués sur le pattern existant `getCachedResearch`/`setCachedResearch`, avec une `apifyMemCache` per-instance gardée en hot-path (évite un round-trip Upstash pour les hits dans la même instance) et Upstash (`tr:apify:<source>:<key>`, EX = TREND_CACHE_SECONDS = 6h) en source de vérité cross-instance. Les 3 Map locales (`tiktokApifyCache`, `twitterApifyCache`, `instagramApifyCache`) sont supprimées ; chaque source (`tiktokViaApify`, `twitterTrends`, `instagramTrends`) passe par les helpers avec clé `tiktok:<country>` / `twitter:<niche>` / `instagram:<niche>`. Garantie après fix : **1 run max / (source, key) / 6h toutes instances confondues** quand Upstash configuré → worst-case journalier ~$0.07 (40 niches × 3 sources × 4 runs/jour × $0.45 moy/1000) = ~70 jours de buffer sur $5. Sans Upstash : fallback Map per-instance préservé (comportement avant fix, vulnérable aux cold starts comme avant). Confirmation parallèle "zéro run sans visite" : pas de `vercel.json` avec crons, pas de `setInterval`/`setTimeout` serveur, seul `/api/trends/route.ts` importe les 3 fonctions Apify et ne s'exécute que sur fetch client, l'ISR `/trends/[niche]` utilise google/YouTube pas Apify, et `revalidate` Next = pull-based (refresh sur requête après TTL, pas en background). Build clean (tsc 0).
- `0b32f4d` — **Instagram ajoutée comme 9e source de trends**, dernière de la salve Apify (TikTok + X + Instagram, même session, même décision user "élargir la breadth multi-plateforme" malgré la mémoire stratégique "freeze features, fix positioning"). Architecture : `lib/trends.ts:instagramTrends(nicheSlug?)` = niche-aware géoless, posts **PUBLICS uniquement** (pas de profils privés, pas de stories, pas de DM — données accessibles à n'importe quel visiteur sans login). Appel isolé derrière `fetchInstagramViaProvider(search)` pour pouvoir swapper Apify → Graph API si Meta rouvre la hashtag search un jour, ou vers un autre scraper. Actor officiel `apify~instagram-scraper` avec input `{search:[hashtags-sans-#], searchType:"hashtag", resultsType:"posts", resultsLimit:30, addParentData:false}`. `INSTAGRAM_NICHE_HASHTAGS` = map hardcodé des 10 niches → 3 hashtags chacun **sans le préfixe `#`** (Apify quirk : input attend le nom du hashtag, pas la string searchable) ; sans niche → fallback `["reels"]`. Lecture défensive de tous les champs (`caption/url/ownerUsername/likesCount/commentsCount/videoViewCount/timestamp/hashtags`). Mapping : `title` = caption tronquée word-boundary à 100 char via `truncateAtWord()` (réutilisé depuis Twitter), OU `#${firstHashtag}` si pas de légende (cas des posts image-only ou caption-less) — drop si ni l'un ni l'autre disponible. `sub` = `${formatCount(likes)} likes · Instagram` SAUF si `likesCount === -1` (IG renvoie -1 quand le poster a caché ses likes via le setting privacy introduit 2019) → fallback `"Instagram"` seul pour ne pas afficher "-1 likes" sur la carte. Cache `Map` per-instance 6h keyed par niche (différentes niches = différentes searchTerms = différents résultats, pas de collision). UI : badge `📸` Instagram pink `#E1306C` (iconique brand, distinguable de TikTok rose `#FE2C55` grâce à l'emoji), banner diplomatique mirror TikTok/X/Reddit/YouTube, **OFF par défaut dans `DEFAULT_SOURCES`** (APIFY_TOKEN payant + actor IG plus cher ~$1.50/1000 vs TikTok/X ~$0.25/1000, donc protection facture renforcée). Géoless (l'actor IG n'a pas de country filter) → ajout à `allGeoLess` UI ET à la condition `sourceGeo = "GLOBAL"` de velocity route. Cap à 30 (pas 50 comme les autres scraping) — input spec actor, moins cher per-run. Note inconsistance acceptée : TikTok `sub` = "1.2M views" (sans "· TikTok"), X = "12k likes · X", Instagram = "123 likes · Instagram" — le badge fait la différence visuellement mais les 3 sources n'ont pas le même format de sub. À unifier dans une passe future si ça gêne en production. Build clean (tsc 0).
- `0b32f4d` — **X (Twitter) ajoutée comme 8e source de trends**, dans la foulée de TikTok (même session, même décision user "élargir la breadth multi-plateforme" — pushback fait, user confirme). Architecture : `lib/trends.ts:twitterTrends(nicheSlug?)` = niche-aware géoless. L'API X officielle est payante depuis 2023 donc scraping tiers obligé ; appel isolé derrière `fetchTwitterViaProvider(searchTerms)` pour pouvoir swapper Apify → reseller REST (GetXAPI, TwitterAPI.io) sans toucher le reste de la source — quand X rentre dans un budget Twitter API ou qu'un reseller devient moins cher, c'est 10 lignes à changer. Actor `apidojo~twitter-scraper-lite` (le plus économique pour de la recherche par mots-clés en petit volume), input `{searchTerms, sort:"Top", maxItems:50, tweetLanguage:"en"}`. `TWITTER_NICHE_TERMS` = map hardcodé des 10 niches → 3 hashtags chacun (#fitness/#fitnessmotivation/#workout, #finance/#investing/#money, etc.) ; sans niche → fallback `["#trending"]`. Lecture défensive de tous les champs (`text/url/twitterUrl/likeCount/retweetCount/viewCount/author.userName/createdAt`) car le schéma actor drift entre versions ; le mapping actuel consomme seulement `text` + `likeCount` mais l'interface déclare tout pour faciliter une évolution future. Titre = tweet tronqué word-boundary à 100 char + "…" via `truncateAtWord()` (fallback char-boundary si la coupe sur le dernier espace laisse <60% du budget, anti-mot-très-long-au-début) ; sub = `${formatCount(likes)} likes · X`. Cache `Map` per-instance 6h keyed par niche (différentes niches = différentes searchTerms = différents résultats, pas de collision). UI : badge `𝕏` (emoji X stylisé Unicode) avec couleur Twitter blue legacy `#1DA1F2`, banner diplomatique mirror TikTok/Reddit/YouTube, **OFF par défaut dans `DEFAULT_SOURCES`** (APIFY_TOKEN payant, mêmes raisons que TikTok). Géoless (l'actor n'a pas de country filter) → ajout à `allGeoLess` côté UI ET à la condition `sourceGeo = "GLOBAL"` de velocity côté route. Refactor de cohérence : `formatViews(n)` (introduit avec TikTok) renommé `formatCount(n)` retournant juste le nombre ("1.2M"/"12k"/"234"), chaque caller compose son unité — évite la duplication entre TikTok ("views") et X ("likes"). Build clean (tsc 0).
- `0b32f4d` — **TikTok ajoutée comme 7e source de trends** (two-step direct → Apify fallback) + nouveau helper `lib/apify.ts`. Motivé par la demande user d'élargir la breadth multi-plateforme malgré la mémoire stratégique "freeze features, fix positioning" — pushback honnête fait avant exécution, user confirme. Architecture : `lib/apify.ts` = thin REST client générique pour l'endpoint sync `api.apify.com/v2/acts/<id>/run-sync-get-dataset-items` (aucune dépendance npm, retourne directement le dataset array, convention `throw "missing-key"` si `APIFY_TOKEN` absent, generic `<T = unknown>` pour type safety côté caller, `cache: "no-store"` car runs Apify pas idempotents). `lib/trends.ts:tiktokTrends(geo)` = (1) tente `ads.tiktok.com/creative_radar_api/v1/popular_trend/hashtag/list` avec UA browser + Referer Creative Center, parse `{code:0, data:{list:[…]}}` → `#hashtag` + `formatViews(video_views)` ; (2) si échec/vide, fallback Apify actor `doliz~tiktok-creative-center-scraper` avec lecture **défensive** des champs (`hashtagName ?? name ?? title` + `video_views ?? views ?? playCount`) car le schéma drift entre versions de l'actor ; (3) si les deux échouent → `throw "missing-key"`. Cache Apify per-instance 6h (Apify = payant, ~$0.25/1000 runs). UI : badge 🎵 rose-brand `#FE2C55`, banner diplomatique mirror Reddit/YouTube, **OFF par défaut dans `DEFAULT_SOURCES`** pour protéger la facture sur un produit zéro utilisateur (recommandation pushback acceptée). Géo-aware (TikTok demande un country_code, `GLOBAL` → fallback US). Route : amélioration du catch parent pour détecter le sentinel `"missing-key"` et mapper à `configured: false` (au lieu de `true` + console.error bruyant) — bénéfice gratuit pour les futures sources qui suivront ce contrat. Fix bonus inclus : `parseSourcesCsv` du localStorage validait uniquement google/youtube/reddit → wikipedia/hackernews/bluesky étaient silently dropés au reload depuis leur ajout, maintenant tout le `Source` union est accepté. Build clean (tsc 0).
- `a3fa8f5` — **Couche SEO technique complète** (max keywords pour le référencement, demande user) : la metadata du site était quasi-vide (`app/layout.tsx` = juste title + 1 phrase ; les pages client home/analyzer/generator/trends/pricing n'héritaient que de ça ; aucun sitemap ni robots). Fix end-to-end : (1) **`app/layout.tsx`** = metadata racine complète — `metadataBase` (`https://hookviral.ai`), title template `"%s | HookViral AI"`, description riche, **keywords array exhaustive** (~30 termes : hook generator / hook analyzer / tiktok-reels-shorts / retention / faceless / scroll-stopping …), `openGraph` (siteName/locale/url) + `twitter summary_large_image` + `robots` (index/follow + googleBot max-image-preview large / max-snippet -1) + JSON-LD `@graph` (Organization + WebSite + SoftwareApplication, offer price 0). (2) **`app/sitemap.ts`** + **`app/robots.ts`** générés par Next → `/sitemap.xml` (toutes surfaces + fan-out `NICHE_MODES` = 2 URLs/niche, priorités/changefreq calibrées) et `/robots.txt` (allow /, disallow /api, sitemap + host) prérendus au build (vérifié). (3) **`layout.tsx` serveur par page client** (analyzer / generator / trends / trends/research / pricing) — les pages `"use client"` ne peuvent pas exporter `metadata`, donc chacune a un layout serveur wrapper qui porte title/description/keywords/canonical/OG uniques. (4) **`hooks-for/[niche]`** gagne un `keywords` array par niche (n'en avait aucun). Build clean (47 pages, tsc 0). Note : domaine canonique = `hookviral.ai` — à connecter dans Vercel si le site tourne encore sur le preview `hookviral-ai.vercel.app`.

**2026-05-28**
- `c9a030f` — **Niches : Relationships 💕 + Lifestyle 🌿 ajoutées** (séparées, pas fusionnées) — motivées par le dogfooding du founder qui teste HookViral 1 mois sur un podcast faceless IA (féminin, angle relation/lifestyle) ; `faceless` collait mal (orienté narration-documentaire). Auto-propagées partout via `NICHE_MODES` : chips Trends, selects Generator/Analyzer, + 4 nouvelles pages SEO evergreen (`/hooks-for/{relationships,lifestyle}` + `/trends/{relationships,lifestyle}`) via `generateStaticParams`.
- `aa51024` — **Fix Wikipedia "ne montre rien"** : le endpoint Wikimedia pageviews "top" lag ~24-48h, pas 24h (vérifié live : J-1 = 404 jusqu'à agrégation complète bien après minuit UTC, J-2 = 200 fiable). On hardcodait J-1 → 404 → route catch → Wikipedia retournait vide silencieusement chaque jour jusqu'à ~midi UTC. Fix : `wikipediaTrends` walk-back J-1→J-2→J-3, `continue` sur 404, sert le premier jour agrégé. Le format `en.wikipedia` (sans .org) était bon, c'était pas le souci. Confirmé : J-2 retourne 999 articles réels.

**2026-05-27**
- `b5a8fa6` — **Platform psychology injectée partout** : nouveau `lib/platforms.ts` (TikTok / Reels / Shorts, scope short-form only — Reddit/X/LinkedIn/Meta-static reportés post-validation), structure miroir de `lib/niches.ts` avec `slug/label/emoji/tagline/guidance`. Le `guidance` block (Platform name + Core Psychology + Rules) est injecté dans : `/api/generate` (génération platform-aware + per-hook `reasoning` field — 1-sentence rationale qui dit pourquoi le hook tient l'attention, affiché sous les chips patterns dans l'UI Generator), `/api/analyze` (scoring platform-aware — juger un hook TikTok par critères TikTok), `/api/rewrite` (rewrites respectent la psychology — "More cinematic" sur TikTok ≠ sur Reels), et `decodeTrend` dans `lib/trends.ts` (Research hooks platform-aware, default TikTok, signature étendue `decodeTrend(trend, niche, platformSlug = "tiktok")` avec cache key incluant le slug pour éviter cross-platform contamination). Bonus : migration `generate` + `rewrite` du parser fragile `JSON.parse(raw.replace(...))` vers `extractJson` — dette technique réduite, ne reste que `script` à propager.
- `a73748b` — **Trends : sanity-check à l'hydration des sources** — fix robustesse pour éviter l'état "tout broken." Quand la sélection persistée dans `localStorage.hv_sources` ne contient *que* des sources gated (YouTube/Reddit) sans creds dans l'env de déploiement, la page atterissait avec tous les banners actifs et aucun bouton évident pour s'en sortir. Maintenant : au mount-once effect, si aucune des sources sauvées n'est dans `DEFAULT_SOURCES` (zero-auth), on force-merge avec les défauts pour garantir qu'il y a toujours au moins une source qui fonctionne. Les toggles gated du user sont préservés en plus.

**2026-05-25**
- `a889c67` — **3ème PersonaCard sur Home** : feedback user "ceux qui ont une idée de contenu mais ne savent pas par où commencer" non couverts par les 2 cards existantes. Ajout d'une carte intermédiaire entre Hook (Analyzer) et Niche (Trends) → `💡 I have an idea, no hook` accent gold, CTA "Generate 8 hooks →" → `/generator`. Grid minmax 220→175px pour que 3 cards tiennent en une rangée dans la colonne hero-left ~520px sans wrap dégueulasse, auto-fit gère le mobile.
- `defd74f` — **Trends banners diplomatiques** : feedback user "afficher une sorte de msg diplomatique pas exposer que l'on manque les credentials." Réécriture des 3 messages user-facing (YouTube unconfigured, Reddit unconfigured, all-sources-empty) → plus de mention de YOUTUBE_API_KEY, REDDIT_CLIENT_ID/SECRET, OAuth, .env.local, ou reddit.com/prefs/apps. Copy générique "X trends aren't available right now. Other sources are still loading — [hide X] to continue." Le dev garde la visibilité via la `unconfigured: Source[]` array dans la response + CLAUDE.md pour la doc env.
- `d9b0bba` — **Bluesky ajouté comme 4ème source zéro-auth ON par défaut** : suite décision stratégique "promesse B = early-signal aggregator" + direction (a) "ajouter sources pour vraie breadth multi-plateforme." Switch tardif de β à α (Bluesky seul, pas Pinterest) : Pinterest API peut requérir un OAuth user-context (auth_code redirect) plutôt que client_credentials, scope incertain pour `/v5/trends/keywords` — préfère ne pas partir sur une investigation Pinterest avant d'avoir vérifié son flow. `lib/trends.ts` : `blueskyTrends()` via `app.bsky.feed.getFeed` sur `public.api.bsky.app` (no auth — atproto API-first by design), feed URI = "whats-hot-classic", posts tronqués 140 chars, sub `@handle · ↑likes · 💬replies`. `TrendSource` étendu à 6 valeurs. Route API : nouvelle branche bluesky + `sourceGeo` set à GLOBAL pour bluesky (le feed est géoless). Page Trends : 6e toggle entre HN et YouTube, badge 🦋 #0085FF (couleur brand), `allGeoLess` étendu à reddit/HN/bluesky. Threads skipped — l'API Meta n'expose pas de trending content global (correction de mon erreur précédente, l'API était oversold).
- `b9acf40` — **Nav : Analyze passe en position 2, avant Trends** — décision stratégique post-discussion : confirmer Analyzer-first comme positioning ("paste hook → score → fix" = demo wow rapide qui convertit) et reléguer Trends en valeur stratégique secondaire dans la hiérarchie de visibilité. Cohérent avec les commits `02dcf5f` (Analyzer-first repositioning, 2026-05-22) et les PersonaCards Home. `NextStep` garde son ordre pédagogique (Trends → Generate → Analyze → Patterns) — c'est la boucle d'apprentissage, pas la nav header. Aucune autre feature changée — décision explicite "freeze features, fix positioning" prise lors de la session 2026-05-25 (cf. `[[project_strategic_state]]` en mémoire).

**2026-05-24**
- `ef45b12` — **Wikipedia + Hacker News ajoutés comme sources zéro-auth** : suite à validation par user que l'objectif "détecter avant les autres" demande plus de breadth, et que Reddit OAuth bloque l'inscription en pratique. `lib/trends.ts` : `wikipediaTrends(geo)` (Wikimedia REST `/metrics/pageviews/top/{project}/all-access/{y}/{m}/{d}`, projet par langue via map geo→wiki en/fr/es/de, J-1 UTC pour éviter le lag d'aggrégation, filtre namespaces) ; `hackerNewsTrends()` (Algolia HN search `?tags=front_page&hitsPerPage=50`, géoless, signal tech/biz/startup). `TrendSource` étendu à 5 valeurs. Route API : 2 nouvelles branches dans le fan-out parallèle + velocity computeVelocity bucket "GLOBAL" pour HN (Wikipedia garde son geo réel parce que c'est une langue-wiki différente). Page Trends : 5 toggles dans la barre source (ordre canonique : Google → Wikipedia → HN → YouTube → Reddit, free-puis-gated), `DEFAULT_SOURCES = ["google", "wikipedia", "hackernews"]` (3 sources zéro-auth ON par défaut, YT/Reddit OFF, banners on-demand). Variable `redditOnly` renommée `allGeoLess` pour générer correctement (Reddit OR HN seuls = geo désactivé). Badges : Wikipedia 📚 #9CA3AF (slate), HN 🟧 #FF6600 (orange officiel HN).
- `463989a` — **Reddit OFF par défaut sur Trends** : suite à discussion stratégique avec user — "le cœur du produit c'est l'analyse IA, pas l'API." Reddit OAuth en 2026 est trop friction pour un visiteur normal (faut s'enregistrer reddit.com/prefs/apps + se battre avec la Responsible Builder Policy). Décision : Reddit reste dans la liste des sources mais OFF par défaut. `DEFAULT_SOURCES = ["google", "youtube"]` séparé d'`ALL_SOURCES`. Le code OAuth + banner explicatif restent en place, fonctionneront dès que les creds sont set dans `.env.local`. Stratégie associée : avant d'ajouter de nouvelles sources (Wikipedia / HN / autres), valider que la V1 actuelle (Google + YouTube + niche rerank + Research page = trend → context + 5 angles + scored hooks + sources) intéresse 5-10 vrais créateurs. Pas de Wikipedia/HN pour le moment.

**2026-05-23**
- `8ee2f15` — **Reddit OAuth** : "reddit ne fonctionne pas" en prod. Diag : depuis le tightening API mi-2023 (la même vague qui a tué Apollo), Reddit 403/429 les IPs cloud sur le JSON public `/r/popular.json`. Marche encore en local depuis IP résidentielle, casse sur Vercel. Fix : flow OAuth `client_credentials` côté serveur dans `lib/trends.ts` (`getRedditToken()`, token Bearer 1h cache in-memory, refresh -60s avant exp via btoa pour portabilité Node/Edge), URL bascule `www.reddit.com/r/popular.json` → `oauth.reddit.com/r/popular`. Cache `popularCache` in-memory 6h pour éviter de re-buster le cache Next.js fetch à chaque rotation de bearer (qui changerait la cache key). Route API check `REDDIT_CLIENT_ID` + `REDDIT_CLIENT_SECRET` → `configured: false` honnête si manquant, banner orange dédié dans `/trends` qui pointe vers `reddit.com/prefs/apps`.
- `8153a2e` — **Trends multi-source** : feedback user "google ne montre que 10 trends" + "tu ne peux sélectionner qu'un filtre à la fois, c'est mauvais". Refactor du sélecteur source : `Source` unique → `Source[]` canonique, toggle indépendant par source (✓ devant l'actif), défaut = les 3 ON pour donner immédiatement la breadth max. `/api/trends?sources=google,youtube,reddit` fan-out parallèle, computeVelocity par source en parallèle, merge round-robin + dedupe titre lowercased. Carte trend porte maintenant un `source` badge coloré (Google bleu / YouTube rouge / Reddit orange) pour distinguer la provenance après merge. `rerankForNiche` signature changée : `(nicheSlug, trends, cacheTag)` — cacheTag opaque (caller construit `sorted-sources:geo`) parce que le cache key précédent `${source}:${niche}:${geo}` ne savait pas représenter une combinaison de sources. Banner "YOUTUBE_API_KEY manquante" devient per-source (warning seulement si YouTube est dans la sélection ET unconfigured). Persistence ajoutée via `getSourcesPref`/`setSourcesPref` dans `lib/prefs.ts`. Garde "ne jamais désélectionner la dernière source". Auto-reset YouTube+GLOBAL retiré : YouTube degrade silencieusement à US côté API quand on lui passe GLOBAL, et avec multi-source l'auto-reset bloquerait l'utilisateur de voir Google/Reddit en Global.
- `114b85a` — **Trends fix triple** (1 commit, 3 bugs liés) : (1) **hydration mismatch sur `/trends`** : `useState(() => typeof window !== 'undefined' ? getGeoPref()… )` lisait localStorage à SSR time → server rendait `geo="US"` mais client hydratait avec la valeur sauvée (ex: `"ES"`). React 19 discard tout le tree à la moindre mismatch. Fix : init avec `"US"` / `""` SSR-safe, hydrate dans `useEffect` mount-once, garde `hydrated` sur les effects de fetch + writeback pour éviter double-fetch et clobber des prefs. (2) **`rerankForNiche` cache key ignorait le geo** (`source:nicheSlug` seulement) → quand une niche était sélectionnée et qu'on changeait le geo, le cache servait stale → "même trends peu importe le filtre". Fix : `source:nicheSlug:geo`, signature étendue, route API met à jour les 3 appels (youtube/reddit/google). (3) **Migration `rerankForNiche` vers `extractJson`** : le parser `JSON.parse(raw.replace(/```json|```/g, ""))` cassait dès que Claude wrappait sa réponse, faisait fallback silencieux au feed brut — possible cause additionnelle du même symptôme. Dette technique flaggée dans CLAUDE.md.
- `0f47781` — **Lighthouse dark-mode contrast + legacy JS** : second pass après le rapport prod 100/100/100/**95**. Tous les échecs color-contrast restants étaient en dark mode (Lighthouse audite en thème par défaut). Fix CSS vars dans `:root` / `[data-theme="dark"]` : `--muted #46465A → #7C7B98` (ratio 2.20 → ~4.7 sur #0c0c14), `--electric #6C3AFF → #8B6BFF` (3.41 → ~5.15). `--soft #8A88A8` laissé inchangé (passait déjà ~5.45). Bonus : ajout `browserslist` dans `package.json` (chrome ≥93 / ff ≥92 / safari ≥15.4 / edge ≥93) pour skip les polyfills bundlés par défaut (Array.at, Array.flat/flatMap, Object.fromEntries/hasOwn, String.trimEnd/trimStart) — gain ~14 KB sur `chunks/16g…js`. Cible visée : Lighthouse 100/100/100/100 prod.
- `4eabd8b` — **Lighthouse a11y fixes (light mode only)** : light-mode CSS vars darkened pour passer WCAG AA 4.5:1 (`--muted` 3.09 → 5.5 ratio, `--neon` 2.66 → 4.8, `--gold` 2.68 → 5.0, `--hot`/`--electric` ajustés en cohérence) ; PersonaCard h3 → h2 (était h1→h3, skip h2 = heading-order violation). Dark mode pas encore touché à ce moment-là — corrigé dans le commit suivant ci-dessus. Mesuré post-push : prod 100/100/100/**95**, Core Web Vitals quasi-parfaits. Le dev mode test mentait (87 perf) — désormais convention : Lighthouse sur prod uniquement.
- `b6a80a1` — **React 19 lint fixes** : 4 erreurs `react-hooks/set-state-in-effect` + 1 `react/no-unescaped-entities`. Themeprovider refactoré vers `useSyncExternalStore` (bonus : cross-tab theme sync via storage events). Nav menu close via `onClick` sur chaque mobile Link (au lieu d'un effect sur path change). `app/history` suppression locale du rule avec justification (mount-once localStorage read). `app/why-it-works` apostrophe escapée. Exit code lint = 0.
- `e70d5c5` — **Nav cleanup** : Logo wordmark rendait le texte `HookViral.ai` dur dupliqué dans la nav header. Retiré, gardé juste suffixe `.ai` discret après le Logo.
- `cfee29d` — **Logo wordmark + favicon redesign** : user feedback "vraiment moche" sur HV monogram. Remplacé par wordmark typo "Hook" gradient + "Viral" text-color, DM Sans 800. Favicon = single H gradient dans rounded square noir (lisible 16x16). Indemodable, style Linear/Vercel/Notion startup-era.
- `9e54ee3` — **PersonaCard border shorthand fix** : React warning sur le mélange `border:` + `borderLeft:`. Split en borderTop/Right/Bottom explicites, borderLeft séparé. Pas de changement visuel.
- `e88c59e` + `41d3401` — **Home : 2 chemins persona** sous le hero. Bordered cards (Border-left 3px hot/electric, hover translateY + glow), chacune clickable en entier : "✦ I have a hook → Score it" → `/analyzer` et "🔬 I have a niche, no idea → Discover trends" → `/trends`. Ferme le gap UX créé par l'Analyzer-first repositioning (un user sans hook ne savait plus où commencer). Premier essai (`41d3401`) trop discret ("ne se voit presque pas en scrollant" feedback user), upgrade en vraies cards bordées (`e88c59e`).
- `b4f8b1a` — **Tighten subtitle** : 33 → 28 mots, rythme X/Y/Z. "Paste any opening line. Get the score, the missing patterns, and a stronger rewrite in seconds — no prompting required." Garde "no prompting required" (le diff vs ChatGPT) au lieu du générique "Stop guessing what works" suggéré par Gemini.

**2026-05-22**
- `02dcf5f` — **Analyzer-first repositioning (3 sharpening moves)** : suite à une critique externe Gemini Pro (dont 4/5 points décrivaient des features déjà implémentées). Les 3 ajustements valides extraits : (1) nav order swap `Generate ↔ Analyze` — workflow naturel = "j'ai un hook → score-le" avant "page blanche → génère". (2) "Start Free →" CTA renommé "Score my hook →" et pointe vers `/analyzer` (était `/generator`) — mobile drawer + desktop nav, alignement avec l'Analyzer comme flagship. (3) Hero copy : "Creators lose viewers in the first 3 seconds. Fix yours." → "Score your hook in **5 seconds**. Fix it in one click." Subtitle remise pour matcher : "Paste your opening line. The Analyzer scores retention, names the attention patterns it's missing, and rewrites it stronger — without prompting." Pas de refonte produit, juste de l'alignement de positionnement.
- `346902d` — **Strategic Takes + Trends Research page + Reddit/Global sources** (gros commit, 5 moves connectés autour du flow trend → research → hook) :
- **🎯 Strategic Takes dans l'Analyzer** : le différenciant catégoriel le plus difficile à copier. Quand l'utilisateur paste un **sujet** (`looksLikeTopic` retourne true), au lieu du score 12/100 inutile l'Analyzer branche vers `/api/strategic-takes` : Claude + `web_search_20250305` (max 3 searches, max_tokens 3000) produit 2-3 **positions opposées défendables** (emoji + name "The takedown" / "The defense" / "The data view" + stance + reasoning + 3-5 arguments concrets avec stats/noms/citations + 2-3 hooks scorés chacun avec patterns chips) + sources URLs. Reasoning par take : pourquoi cette position win attention. Border-left colorée par take (rouge/électrique/or) pour distinguer les camps visuellement. Override "Score as a hook anyway →" pour les false positives du detector (ex: "this changes everything" passe à travers la garde). Loading state distinct ("Researching positions… ~10s"). SessionStorage `hv:takes:<subject>::<niche>` pour back-nav instantané + Upstash 6h pour partage cross-users. C'est la feature qui repositionne le produit : pas "AI hook generator" mais "tool qui te dit comment penser un sujet pour qu'il performe".

**2026-05-21**
- **Reddit source + Global geo (multi-source breadth)** : suite à la découverte que **Google a décommissionné ses APIs JSON (dailytrends + realtimetrends = 404 testés live)** et que le RSS Google ne renvoie plus que **10 items par geo, point**, on étend la breadth par deux moves : (1) pseudo-geo "🌍 Global" qui fan-out 6 geos en parallèle (US/GB/CA/AU/DE/FR) avec dedupe par title lowercased → ~30 items Google uniques au lieu de 10. (2) Nouvelle source **🟠 Reddit /r/popular** (JSON public, no key, NSFW/stickied filtrés, sub = "r/community · 12.3k upvotes") → 30-50 posts trending toutes communautés avec un signal vraiment différent (cultural buzz vs search queries). UI : Reddit ajouté au source row, Global ajouté en tête du geo row, geo désactivé visuellement quand source=reddit (Reddit ignore le geo_filter depuis une IP serveur — testé). YouTube + Global = auto-reset US (YouTube API ne supporte pas notre pseudo-geo).
- **Trends caps unlock** : feedback user "10 trends ne suffisent pas, il faut toucher un maximum de sujet". Tout était coupé à 12 alors que les sources nous donnent plus. `googleTrends` 12 → 30 (cap safety, source réelle = 10). `youtubeTrends` `maxResults` 20 → **50** (cap API officiel), slice 20 → 50. `rerankForNiche` slice 12 → 40 + max_tokens 700 → 1200 (sinon le JSON tronquait sur larger reranks et tout retombait au feed brut, silencieusement). `/api/trends` retire son re-cap à 12 sur le path YouTube. UI : compteur "N trends" toujours visible à droite de la barre de filtre (était caché tant qu'aucune query).
- **Research depuis l'Analyzer + sessionStorage hydration** : ferme la boucle hook → research. `/api/analyze` retourne maintenant un `subject` (2-6 mots searchables, vide si pas extractible). Le résultat Analyzer affiche un callout gradient "🔬 Push it further — Deep research on 'Mbappé refusal PSG 2017'" linkant vers `/trends/research?q=<subject>` (même page que depuis Trends). Plus : `/trends/research` hydrate désormais depuis `sessionStorage.getItem("hv:research:<q>::<niche>")` à l'initial render → restauration instantanée zero-flash quand tu reviens depuis Analyzer/Generator. Cache écrit après chaque fetch réussi. Bouton "← Back to trends" passé de `router.back()` à `<Link href="/trends">` fixe (prévisible peu importe d'où tu viens). `/api/analyze` migre aussi vers `extractJson` (le parsing fragile était partout).
- **Trends Research → page dédiée** : feedback user "le visuel en dépliant dans une carte de 320px sur PC n'est pas encourageant à lire". Nouvelle route `app/trends/research/page.tsx` plein écran, layout 2 colonnes (Context sticky 42% / Angles 58% desktop, stack mobile). La carte trend perd toute la logique d'expansion in-card (state machine, AngleCard inline, sources inline, ProNote, decode credits) — `app/trends/page.tsx` -185 lignes. Le bouton 🔬 Research devient un `<Link>` simple gradient promu en CTA principal de la carte ; ⚡ Quick hooks (Generator) + ✦ Score it (Analyzer) sous-titres discrets. Loading state explicite avec nom du trend + `router.back()` pour retour grille (préserve scroll).
- `d9a22ef` — **Trends Research (web_search) + Decode 500 fix + Analyzer topic detector** (1 commit, 3 changements liés au flow trend→contenu cassé) :
  - **Trends Research** : remplace "Decode into content angles" par "🔬 Research & angles" — Claude + `web_search_20250305` tool (max 3 searches/call). Au lieu de deviner les angles depuis le nom du trend, Claude fait des recherches web réelles puis synthétise. Output : `context {who/what/whyTrending/stakes/timeline}` + 3-5 angles scorés avec `reasoning` chacun + `sources[]` URLs cliquables. Pro visuelle (badge sur bouton, ouvert tant qu'`isPro()=true`). Cache 6h Upstash + mémoire par (niche, trend) → 1 appel web_search partagé entre tous les users sur le même bucket.
  - **Fix Decode 500** : 2 causes — max_tokens 700 trop court (output tronqué mid-JSON sur 3 angles + arrays) + parsing fragile `JSON.parse(raw.replace(/```json/, ""))` qui explosait dès que Claude ajoutait une intro. `lib/parseJson.ts` `extractJson<T>` extrait le premier bloc `{…}` ou `[…]` équilibré (gère fences, intro, escapes). max_tokens 700 → 3000 (research = tool use coûte des tokens internes).
  - **Topic detector sur Analyzer** : heuristique `looksLikeTopic()` (≤6 mots, pas de ponctuation, pas de pronom personnel ni verbe impératif) → quand score <40 ET input ressemble à un topic, callout doré "💡 reads like a topic, not a hook — Generate 8 hooks for this →". Empêche les users de rester bloqués face à un 12/100 sans path de sortie (cas signalé : "meghan markle england visit" collé dans Analyzer).
- Ajout de la section "Journal des changements" dans CLAUDE.md (CHANGELOG.md séparé envisagé puis abandonné — préférence user : tout reste dans un seul doc).
- `88171b6` — Refactor CLAUDE.md : structure durable + une seule section "État actuel". 200 → 90 lignes. Le journal repart d'ici (cette section) au cas où.

**2026-05-20**
- `cb08ee9` — **Simplification radicale (4 moves)** : Pro plumbing caché (`isPro()` = true en attendant Stripe) · Home trimée 10 → 5 sections (cut Typing demo, Examples, Features, Trends teaser, FAQ, real-run callout) · Defaults > knobs sur Generator/Analyzer/Trends (chips sous `<details>More options ▾</details>`) · `/why-it-works` réécrit en guide "Start here" plain language (4 outils × What/When/Why). Net : +208/−682 lignes.
- `7abede7` — **Squelette + boucle + connexion** (30 fichiers, +2110/−378) : nav flat (Home·Trends·Generate·Analyze·Patterns·Pricing·History), `NextStep` partout (+ mode outside-loop), Patterns hub, **Trends v2** (decode → angles + velocity + sparklines + faceless brief + niche re-rank + geo selector), **Analyzer** (targeted rewrite par pattern manquant — 12→84, niche-aware, 3s check, deep-links /patterns#id, Before/After), **Generator** patterns visibles + deep analyze, `/why-it-works` article, `/api/subscribe` Upstash email capture, `ProLock` unifié, `HomeAnalyzeWidget` inline, prefs cross-page (`lib/prefs.ts`).

**2026-05-18** — Sprint Phases 0 → 3
- `72bd1da` — Logo HV monogram (gradient brand).
- `1fdbf34` — Homepage : live Trends + FAQ, nav dropdown "More".
- `af6d79d` — Couche Patterns : taxonomie possédée 9 attention patterns (moat v1).
- `6384da8` — Décision : Stripe reporté jusqu'à Phase 4 (post-auth + rétention prouvée).
- `b13c68b` — Roadmap : statut Phase 3 (Rewrite ✅ / Stripe bloqué).
- `75a63d6` — Phase 3 : Rewrite engine (`/api/rewrite`, 5 styles, 3 variantes).
- `3c04eb7` — Phase 2 complete : niche re-ranking Claude + pages SEO `/trends/[niche]`.
- `eed1c91` — Trends : source Google RSS gratuite (zéro clé).
- `063dcc2` — Phase 2 : niche modes, pages SEO `/hooks-for/[niche]`, section Trends double-source.
- `15328cc` — Phase 1 : Hook Analyzer autonome, repositionnement rétention, `claude-sonnet-4-6` sur generate/script/analyze.
- `ea3c393` — Phase 0 : rendu des hooks dans Generator, fuite de crédits corrigée, rate-limiting serveur par IP.

**2026-03 → 2026-04** — Build initial (avant Phase 0)
- Bootstrap Next.js, routes API `generate` + `script`, pages Generator / History / Pricing / Privacy / Terms, composant `Nav`, `lib/credits.ts`, premiers essais SEO TikTok.

### Convention pour la suite
Chaque commit significatif sur `main` reçoit une ligne ici, le jour du commit, en haut de la section date courante. Une ligne suffit — le détail vit dans le message de commit. Refactors cosmétiques / typo fixes peuvent être omis.
