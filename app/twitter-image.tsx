// Same card as the OpenGraph image — Next only injects twitter:image from
// this file convention, it does not fall back to opengraph-image.
export { default, alt, size, contentType } from "./opengraph-image";
